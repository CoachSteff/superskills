{% extends "base.html.j2" %}

{% block content %}
<div style="text-align: center;">
    <h1>{{ slide.heading }}</h1>
    {% if slide.subheading %}
    <p style="font-size: calc(var(--body-size) * 1.2); color: var(--color-text-secondary); margin-top: 1em; font-weight: 300;">
        {{ slide.subheading }}
    </p>
    {% endif %}
</div>
{% endblock %}
